<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <meta http-equiv="Content-Security-Policy" content="style-src 'self' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com;">
    <link href="https://fonts.googleapis.com/css2?family=League+Spartan&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="../estilos/Registro_Hilos_Style.css">
    <title>Registro</title>
</head>
<body>

    <div class="container">
        <div class="menu_right"><a href="/"><input class="btn_menu" type="submit" value="Atras"></a></div>

        <div class="title">
            <div class="menu_center"><a class="ugc">Registro de usuarios</a></div>
            
        </div>
        <div class="section">
            <a class="page"> Registrarse</a>
        </div>
        <form action="/register" method="POST">
            <div class="form-group">
                <input type="text" name="name" id="name" placeholder="Full Name" class="form-control" autofocus> 
            </div>
            <div class="form-group">
                <input type="password" id="password" name="password" placeholder="Password" class="form-control">
            </div>
            <div class="form-group">
                <button class="btn btn-success btn-block">
                    Registrar
                </button>
            </div>
        </form>
    </div>
    <!--<% if(typeof alert != "undefined") { %>
        <script>
            Swal.fire({
                title: '<%= alertTitle %>' ,
                text: '<%= alertMessage %>',
                icon: '<%= alertIcon %>',
                showConfirmButton:'<%=showConfimButton%>' ,
                timer: '<%=timer%>'
            }).then(()=>{
                window.location = '/Login <%= ruta%>'
            });
        </script>
    <%}%>  -->
       
</body>
</html>

